# Task Backlog

Add tasks below as unchecked items under **Open**. A scheduled agent checks this file every 4 hours. If there's an open item, it reads [CLAUDE.md](CLAUDE.md) for the repo's guardrails, works the task on a fresh branch, runs `npm run preflight`, pushes, and opens a PR for review — it never merges, never pushes to `main`, and never touches site content/copy/design/styling unless the task text explicitly authorizes that exact change. If there's nothing open, it does nothing.

Be specific in each item — the agent has no memory of this conversation or any other; it only has what's written here plus the repo itself.

## Open

- [ ]

## Done
